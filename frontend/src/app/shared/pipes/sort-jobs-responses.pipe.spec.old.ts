import { SortJobsResponsesPipe } from './sort-jobs-responses.pipe';

describe('SortJobsResponsesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortJobsResponsesPipe();
    expect(pipe).toBeTruthy();
  });
});
